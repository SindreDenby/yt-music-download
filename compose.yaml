services:
  downloader:
    build: .
    image: yt-music-downloader
    ports:
      - "127.0.0.1:8765:8765"
    volumes:
      - downloads:/data
    init: true
    restart: unless-stopped

volumes:
  downloads:
