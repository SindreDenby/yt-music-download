services:
  downloader:
    build: .
    image: yt-music-downloader
    ports:
      - "8765:8765"
    volumes:
      - downloads:/data
    init: true
    restart: unless-stopped

volumes:
  downloads:
